# Планирование проекта

## Использование агента планирования

Переключитесь на режим планирования:

```
> /plan
# или нажмите Tab
```

## Планирование архитектуры

### Анализ требований

```
> Мне нужно создать todo приложение со следующими функциями:
> - Добавление задач
> - Редактирование задач
> - Удаление задач
> - Отметка выполненных
> - Фильтрация (все/активные/выполненные)
> - Сохранение в localStorage
>
> Спроектируй архитектуру и предложи структуру компонентов.
```

### Результат планирования

Claude Code предложит структуру:

```
📁 Компоненты:
├── App.tsx              # Главный компонент-контейнер
├── TodoList.tsx         # Список задач
├── TodoItem.tsx         # Отдельная задача
├── TodoForm.tsx         # Форма добавления
├── TodoFilters.tsx      # Фильтры
└── TodoStats.tsx        # Статистика

📁 Хуки:
├── useTodos.ts          # Управление задачами
├── useLocalStorage.ts   # Сохранение в localStorage
└── useFilteredTodos.ts  # Фильтрация

📁 Типы:
└── types.ts             # Todo, Filter, и другие типы
```

## Планирование типов данных

```
> Определи TypeScript типы для todo приложения
```

### Результат

```typescript
// types.ts
export interface Todo {
  id: string;
  title: string;
  completed: boolean;
  createdAt: Date;
  updatedAt: Date;
}

export type Filter = 'all' | 'active' | 'completed';

export interface TodoState {
  todos: Todo[];
  filter: Filter;
}

export type TodoAction =
  | { type: 'ADD'; payload: Omit<Todo, 'id' | 'createdAt' | 'updatedAt'> }
  | { type: 'UPDATE'; payload: Partial<Todo> & { id: string } }
  | { type: 'DELETE'; payload: string }
  | { type: 'TOGGLE'; payload: string }
  | { type: 'CLEAR_COMPLETED' }
  | { type: 'SET_FILTER'; payload: Filter };
```

## Планирование API хуков

```
> Спроектируй хуки для управления состоянием todo
```

### Результат

```typescript
// hooks/useTodos.ts
import { useReducer, useEffect } from 'react';
import { Todo, TodoAction, Filter } from '../types';

interface State {
  todos: Todo[];
  filter: Filter;
}

// reducer, actions, и хук
```

## Планирование UI компонентов

### TodoForm

```
> Опиши структуру компонента TodoForm
```

### TodoList

```
> Опиши структуру компонента TodoList
```

### TodoItem

```
> Опиши структуру компонента TodoItem
```

## Планирование маршрутов (если есть)

```
> Если приложение будет многостраничным, спланируй маршруты:
> - / — список задач
> - /task/:id — детали задачи
> - /settings — настройки
```

## Планирование тестов

```
> Спланируй тесты для каждого компонента и хука
```

### Результат

```
📁 Тесты:
├── components/
│   ├── TodoForm.test.tsx
│   ├── TodoList.test.tsx
│   ├── TodoItem.test.tsx
│   └── TodoFilters.test.tsx
├── hooks/
│   ├── useTodos.test.ts
│   └── useLocalStorage.test.ts
└── utils/
    └── filterTodos.test.ts
```

## Планирование стилей

```
> Предложи подход к стилизации:
> - CSS Modules
> - CSS переменные для темы
> - Адаптивность
> - Анимации
```

## Планирование данных

### Структура localStorage

```
{
  "todos": [
    {
      "id": "uuid-1",
      "title": "Изучить Claude Code",
      "completed": false,
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2024-01-01T00:00:00Z"
    }
  ],
  "settings": {
    "theme": "light",
    "sort": "date"
  }
}
```

## Планирование функционала по итерациям

### Итерация 1: Базовый CRUD
- [ ] Отображение списка задач
- [ ] Добавление задачи
- [ ] Удаление задачи
- [ ] Отметка выполненных

### Итерация 2: Фильтрация
- [ ] Фильтр: все/активные/выполненные
- [ ] Счётчики
- [ ] Очистка выполненных

### Итерация 3: Сохранение
- [ ] Сохранение в localStorage
- [ ] Загрузка при старте
- [ ] Синхронизация

### Итерация 4: UX улучшения
- [ ] Drag & drop сортировка
- [ ] Inline редактирование
- [ ] Анимации
- [ ] Горячие клавиши

### Итерация 5: Дополнительно
- [ ] Темы (светлая/тёмная)
- [ ] Экспорт/импорт
- [ ] Синхронизация между вкладками

## Сохранение плана

```
> Сохрани план разработки в файл PLAN.md
```

### Пример PLAN.md

```markdown
# План разработки Todo приложения

## Архитектура
- React 18 с хуками
- TypeScript
- CSS Modules
- localStorage для хранения

## Итерации

### Итерация 1 (Базовый CRUD)
- TodoList компонент
- TodoForm компонент
- useTodos хук

### Итерация 2 (Фильтрация)
- TodoFilters компонент
- useFilteredTodos хук
- TodoStats компонент

### Итерация 3 (Сохранение)
- useLocalStorage хук
- Автосохранение

## Компоненты
...

## Типы
...
```

## Переключение на сборку

После планирования переключитесь на режим сборки:

```
> /build
# или нажмите Tab
```

## Следующий шаг

Теперь у вас есть план. Перейдите к [итеративной разработке](2.3-build-iterate.md).