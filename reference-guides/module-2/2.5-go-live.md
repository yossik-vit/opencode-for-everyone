# Развёртывание проекта

## Подготовка к продакшн

### Проверка качества

```
> Проверь проект перед деплоем:
> - Все тесты проходят
> - Нет ошибок линтера
> - TypeScript компилируется
> - Нет console.log в коде
> - .env файлы не в репозитории
```

### Оптимизация

```
> Оптимизируй проект для продакшн:
> - Минификация
> - Tree shaking
> - Code splitting
> - Оптимизация изображений
```

## Выбор хостинга

### Для статических сайтов (SPA)

| Платформа | Бесплатно | Особенности |
|-----------|-----------|-------------|
| Vercel | ✅ | Лучший для Next.js/React |
| Netlify | ✅ | Лучший для JAMStack |
| GitHub Pages | ✅ | Для Open Source |
| Cloudflare Pages | ✅ | CDN, быстрый |
| Firebase Hosting | ✅ | Google инфраструктура |

### Для Node.js бэкенда

| Платформа | Бесплатно | Особенности |
|-----------|-----------|-------------|
| Railway | ⚠️ | $5 кредит |
| Render | ✅ | Free tier |
| Fly.io | ⚠️ | $5 кредит |
| Heroku | ❌ | Платный |

## Развёртывание на Vercel

### Установка Vercel CLI

```bash
npm i -g vercel
```

### Авторизация

```bash
vercel login
```

### Деплоем

```bash
vercel
```

### Через Claude Code

```
> Разверни проект на Vercel
> Используй vercel cli
```

### Автоматический деплоем

Vercel автоматически деплоит при пуше в main.

## Развёртывание на Netlify

### Установка Netlify CLI

```bash
npm i -g netlify-cli
```

### Инициализация

```bash
netlify init
```

### Деплоем

```bash
netlify deploy --prod
```

### Через Claude Code

```
> Разверни проект на Netlify
```

## Развёртывание на GitHub Pages

### Настройка

```
> Настрой GitHub Pages для этого проекта:
> - Используй GitHub Actions для деплоя
> - Автоматический деплоем при пуше в main
```

### Workflow

```yaml
# .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

## Развёртывание на Firebase

### Установка Firebase CLI

```bash
npm i -g firebase-tools
```

### Инициализация

```bash
firebase init hosting
```

### Деплоем

```bash
firebase deploy
```

## Настройка домена

### Кастомный домен

```
> Настрой кастомный домен для проекта:
> - DNS записи
> - SSL сертификат
> - Перенаправление www
```

### Проверка DNS

```bash
dig your-domain.com
nslookup your-domain.com
```

## Переменные окружения

### Настройка на платформе

```bash
# Vercel
vercel env add VITE_API_URL

# Netlify
netlify env:set VITE_API_URL "https://api.example.com"

# GitHub Pages (secrets)
gh secret set VITE_API_URL --body "https://api.example.com"
```

### Проверка переменных

```
> Проверь что все переменные окружения настроены для продакшн
```

## Мониторинг

### Настройка мониторинга

```
> Настрой мониторинг для проекта:
> - Sentry для ошибок
> - Google Analytics для аналитики
> - Uptime мониторинг
```

### Health check

```
> Создай endpoint /health для проверки работоспособности
```

## CI/CD Pipeline

### Полный pipeline

```
> Создай полноценный CI/CD pipeline:
> - Lint при PR
> - Test при PR
> - Build при merge в main
> - Deploy при merge в main
> - Notify при ошибках
```

### Пример pipeline

```yaml
# .github/workflows/ci-cd.yml
name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run lint
      - run: npm test
      - run: npm run build

  cd:
    needs: ci
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run build
      - uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID}}
          vercel-project-id: ${{ secrets.PROJECT_ID}}
          vercel-args: '--prod'
```

## Пост-деплоем проверка

### Smoke тесты

```
> Запусти smoke тесты после деплоя:
> - Главная страница загружается
> - API отвечает
> - Основные функции работают
```

### Проверка производительности

```
> Проверь производительность:
> - Lighthouse score
> - Core Web Vitals
> - Time to First Byte
```

## Роллбэк

### Откат изменений

```bash
# Vercel
vercel rollback

# Netlify
netlify deploy --prod --alias=previous

# GitHub Pages
git revert HEAD
git push
```

### Через Claude Code

```
> Откати последний деплоем на Vercel
```

## Документация

### README

```
> Обнови README с информацией:
> - Как запустить локально
> - Как задеплоить
> - Переменные окружения
> - Contributing guide
```

### Деплоем документация

```
> Создай DEPLOY.md с инструкциями по развёртыванию
```

## Финальная проверка

### Чек-лист

- [ ] Все тесты проходят
- [ ] Линтер не выдаёт ошибок
- [ ] TypeScript компилируется
- [ ] Production build работает
- [ ] Деплоем успешен
- [ ] Домен настроен
- [ ] SSL работает
- [ ] Мониторинг настроен
- [ ] Документация актуальна

### Финальный коммит

```
> Сделай финальный коммит "release: v1.0.0"
> Создай тег v1.0.0
> Создай релиз на GitHub
```

## Поздравляем!

🎉 Вы успешно завершили курс "Claude Code для всех"!

### Что вы сделали

1. ✅ Установили и настроили Claude Code
2. ✅ Изучили основы работы
3. ✅ Создали AGENTS.md
4. ✅ Спроектировали архитектуру
5. ✅ Реализовали проект итеративно
6. ✅ Интегрировали с GitHub
7. ✅ Развернули в продакшн

### Что дальше?

- Продолжайте использовать Claude Code в проектах
- Изучите продвинутые функции
- Создавайте пользовательских агентов
- Делитесь опытом с сообществом
- Вносите вклад в Claude Code

Удачи в разработке! 🚀